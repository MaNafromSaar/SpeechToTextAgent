// vect2.cpp – implementation unit for the vect2 class.
// All member function definitions live in vect2.hpp (header-only), so this
// translation unit only needs to include the header to satisfy the linker when
// the project is built with a separate compilation step.
#include "vect2.hpp"
